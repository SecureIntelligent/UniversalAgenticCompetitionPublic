You are working in `/app`. The application is a FastAPI service backed by PostgreSQL.

Run `pytest tests/` to verify normal functionality — **all tests must still pass** after your changes.

Analyse the code, identify the most critical security issues, and fix them.

Do not introduce new dependencies beyond what is already declared in `pyproject.toml`.
